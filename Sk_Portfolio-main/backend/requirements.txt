# Trimmed to exactly what backend/server.py imports + the ASGI server to run it.
# (The previous file pulled in unrelated tooling -- pandas/numpy/black/flake8/mypy/
# boto3/etc -- and a private package (emergentintegrations) that isn't on public
# PyPI at all, which is why the Render build failed.)

fastapi==0.110.1
uvicorn==0.25.0
python-dotenv>=1.0.1
motor==3.3.1
pymongo==4.6.3
pydantic>=2.6.4
email-validator>=2.2.0
python-multipart>=0.0.9
resend==2.32.2
